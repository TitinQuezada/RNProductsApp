import { StyleSheet } from "react-native";

export const CategoryButtonStyles = StyleSheet.create({
    container: {
        borderRadius: 100,
        borderWidth: 1,
        height: 40,

    },
    text: {
        marginHorizontal: 10,
        marginBottom: 'auto',
        marginTop: 'auto',
    }
});